local N_POSES = 3
local USE_POSE = math.random(1, N_POSES)


function STEADY_POSE(heading, pitch)
    if USE_POSE <= 0 or USE_POSE > N_POSES then
        USE_POSE = 1
    end

    if USE_POSE == 1 then
        Turn(base, x_axis, 0.0, math.rad(500))
        Turn(base, y_axis, 0.0, math.rad(500))
        Turn(base, z_axis, 0.0, math.rad(500))
        Turn(pelvis, x_axis, 0.0, math.rad(500))
        Turn(pelvis, y_axis, 0.0, math.rad(500))
        Turn(pelvis, z_axis, 0.0, math.rad(500))
        Turn(lthigh, x_axis, 0.0, math.rad(500))
        Turn(lthigh, y_axis, -0.0, math.rad(500))
        Turn(lthigh, z_axis, 0.0872664600610733, math.rad(500))
        Turn(lleg, x_axis, 0.0, math.rad(500))
        Turn(lleg, y_axis, 0.1745329350233078, math.rad(500))
        Turn(lleg, z_axis, -0.08726644515991211, math.rad(500))
        Turn(rthigh, x_axis, 0.0, math.rad(500))
        Turn(rthigh, y_axis, -0.0, math.rad(500))
        Turn(rthigh, z_axis, -0.0872664600610733, math.rad(500))
        Turn(rleg, x_axis, 0.0, math.rad(500))
        Turn(rleg, y_axis, -0.0872664526104927, math.rad(500))
        Turn(rleg, z_axis, 0.0872664526104927, math.rad(500))
        Turn(torso, x_axis, 0.0, math.rad(500))
        Turn(torso, y_axis, 0.0, math.rad(500))
        Turn(torso, z_axis, 0.0, math.rad(500))
        Turn(chest, x_axis, 0.0, math.rad(500))
        Turn(chest, y_axis, 0.0, math.rad(500))
        Turn(chest, z_axis, 0.0, math.rad(500))
        Turn(head, x_axis, 0.0, math.rad(200))
        Turn(head, y_axis, 0.0, math.rad(500))
        Turn(head, z_axis, 0.0, math.rad(200))
        Turn(head2, x_axis, 0.0, math.rad(500))
        Turn(head2, y_axis, 0.0, math.rad(500))
        Turn(head2, z_axis, 0.0, math.rad(500))
        Turn(head3, x_axis, 0.0, math.rad(500))
        Turn(head3, y_axis, 0.0, math.rad(500))
        Turn(head3, z_axis, 0.0, math.rad(500))
        Turn(helmet, x_axis, 0.0, math.rad(500))
        Turn(helmet, y_axis, 0.0, math.rad(500))
        Turn(helmet, z_axis, 0.0, math.rad(500))
        Turn(shoulders, x_axis, 0.0, math.rad(500))
        Turn(shoulders, y_axis, 0.0, math.rad(500))
        Turn(shoulders, z_axis, 0.0, math.rad(500))
        Turn(luparm, x_axis, 0.0, math.rad(500))
        Turn(luparm, y_axis, -0.0, math.rad(500))
        Turn(luparm, z_axis, 0.1745329201221466, math.rad(500))
        Turn(lloarm, x_axis, -0.05001140385866165, math.rad(500))
        Turn(lloarm, y_axis, -0.0017878736834973097, math.rad(500))
        Turn(lloarm, z_axis, -0.4205205738544464, math.rad(500))
        Turn(ruparm, x_axis, -0.5219494104385376, math.rad(500))
        Turn(ruparm, y_axis, -0.04359167069196701, math.rad(500))
        Turn(ruparm, z_axis, -0.07562289386987686, math.rad(500))
        Turn(rloarm, x_axis, -1.8233871459960938, math.rad(500))
        Turn(rloarm, y_axis, -0.31062978506088257, math.rad(500))
        Turn(rloarm, z_axis, -0.023563174530863762, math.rad(500))
        Turn(gun, x_axis, 0.0, math.rad(500))
        Turn(gun, y_axis, 0.0, math.rad(500))
        Turn(gun, z_axis, 0.0, math.rad(500))
        Turn(flare, x_axis, 0.0, math.rad(500))
        Turn(flare, y_axis, 0.0, math.rad(500))
        Turn(flare, z_axis, 0.0, math.rad(500))

        WaitForTurn(base, x_axis)
        WaitForTurn(base, y_axis)
        WaitForTurn(base, z_axis)
        WaitForTurn(pelvis, x_axis)
        WaitForTurn(pelvis, y_axis)
        WaitForTurn(pelvis, z_axis)
        WaitForTurn(lthigh, x_axis)
        WaitForTurn(lthigh, y_axis)
        WaitForTurn(lthigh, z_axis)
        WaitForTurn(lleg, x_axis)
        WaitForTurn(lleg, y_axis)
        WaitForTurn(lleg, z_axis)
        WaitForTurn(rthigh, x_axis)
        WaitForTurn(rthigh, y_axis)
        WaitForTurn(rthigh, z_axis)
        WaitForTurn(rleg, x_axis)
        WaitForTurn(rleg, y_axis)
        WaitForTurn(rleg, z_axis)
        WaitForTurn(torso, x_axis)
        WaitForTurn(torso, y_axis)
        WaitForTurn(torso, z_axis)
        WaitForTurn(chest, x_axis)
        WaitForTurn(chest, y_axis)
        WaitForTurn(chest, z_axis)
        WaitForTurn(head, x_axis)
        WaitForTurn(head, y_axis)
        WaitForTurn(head, z_axis)
        WaitForTurn(head2, x_axis)
        WaitForTurn(head2, y_axis)
        WaitForTurn(head2, z_axis)
        WaitForTurn(head3, x_axis)
        WaitForTurn(head3, y_axis)
        WaitForTurn(head3, z_axis)
        WaitForTurn(helmet, x_axis)
        WaitForTurn(helmet, y_axis)
        WaitForTurn(helmet, z_axis)
        WaitForTurn(shoulders, x_axis)
        WaitForTurn(shoulders, y_axis)
        WaitForTurn(shoulders, z_axis)
        WaitForTurn(luparm, x_axis)
        WaitForTurn(luparm, y_axis)
        WaitForTurn(luparm, z_axis)
        WaitForTurn(lloarm, x_axis)
        WaitForTurn(lloarm, y_axis)
        WaitForTurn(lloarm, z_axis)
        WaitForTurn(ruparm, x_axis)
        WaitForTurn(ruparm, y_axis)
        WaitForTurn(ruparm, z_axis)
        WaitForTurn(rloarm, x_axis)
        WaitForTurn(rloarm, y_axis)
        WaitForTurn(rloarm, z_axis)
        WaitForTurn(gun, x_axis)
        WaitForTurn(gun, y_axis)
        WaitForTurn(gun, z_axis)
        WaitForTurn(flare, x_axis)
        WaitForTurn(flare, y_axis)
        WaitForTurn(flare, z_axis)
    elseif USE_POSE == 2 then
        Turn(base, x_axis, 0.0, math.rad(500))
        Turn(base, y_axis, 0.0, math.rad(500))
        Turn(base, z_axis, 0.0, math.rad(500))
        Turn(pelvis, x_axis, 0.0, math.rad(500))
        Turn(pelvis, y_axis, 0.0, math.rad(500))
        Turn(pelvis, z_axis, 0.0, math.rad(500))
        Turn(lthigh, x_axis, 0.0, math.rad(500))
        Turn(lthigh, y_axis, -0.0, math.rad(500))
        Turn(lthigh, z_axis, 0.0872664600610733, math.rad(500))
        Turn(lleg, x_axis, 1.8913792754204906e-09, math.rad(500))
        Turn(lleg, y_axis, 0.1745329350233078, math.rad(500))
        Turn(lleg, z_axis, -0.08726644515991211, math.rad(500))
        Turn(rthigh, x_axis, 0.0, math.rad(500))
        Turn(rthigh, y_axis, -0.0, math.rad(500))
        Turn(rthigh, z_axis, -0.0872664600610733, math.rad(500))
        Turn(rleg, x_axis, 9.34880062253285e-10, math.rad(500))
        Turn(rleg, y_axis, -0.0872664526104927, math.rad(500))
        Turn(rleg, z_axis, 0.0872664526104927, math.rad(500))
        Turn(torso, x_axis, 0.0, math.rad(500))
        Turn(torso, y_axis, 0.0, math.rad(500))
        Turn(torso, z_axis, 0.0, math.rad(500))
        Turn(chest, x_axis, 0.0, math.rad(500))
        Turn(chest, y_axis, 0.0, math.rad(500))
        Turn(chest, z_axis, 0.0, math.rad(500))
        Turn(head, x_axis, 3.536157677785923e-08, math.rad(500))
        Turn(head, y_axis, 0.0, math.rad(500))
        Turn(head, z_axis, -4.067665670959286e-08, math.rad(500))
        Turn(head2, x_axis, 0.0, math.rad(500))
        Turn(head2, y_axis, 0.0, math.rad(500))
        Turn(head2, z_axis, 0.0, math.rad(500))
        Turn(head3, x_axis, 0.0, math.rad(500))
        Turn(head3, y_axis, 0.0, math.rad(500))
        Turn(head3, z_axis, 0.0, math.rad(500))
        Turn(helmet, x_axis, 0.0, math.rad(500))
        Turn(helmet, y_axis, 0.0, math.rad(500))
        Turn(helmet, z_axis, 0.0, math.rad(500))
        Turn(shoulders, x_axis, 0.0, math.rad(500))
        Turn(shoulders, y_axis, 0.0, math.rad(500))
        Turn(shoulders, z_axis, 0.0, math.rad(500))
        Turn(luparm, x_axis, -0.4363322854042053, math.rad(500))
        Turn(luparm, y_axis, -9.384009354367436e-17, math.rad(500))
        Turn(luparm, z_axis, -0.2617993950843811, math.rad(500))
        Turn(lloarm, x_axis, -0.03648074343800545, math.rad(500))
        Turn(lloarm, y_axis, 0.17956964671611786, math.rad(500))
        Turn(lloarm, z_axis, -0.3998515009880066, math.rad(500))
        Turn(ruparm, x_axis, -0.4363322854042053, math.rad(500))
        Turn(ruparm, y_axis, 9.384009354367436e-17, math.rad(500))
        Turn(ruparm, z_axis, 0.1745329052209854, math.rad(500))
        Turn(rloarm, x_axis, -0.052524928003549576, math.rad(500))
        Turn(rloarm, y_axis, -0.2129141390323639, math.rad(500))
        Turn(rloarm, z_axis, 0.4820796549320221, math.rad(500))
        Turn(gun, x_axis, 0.0, math.rad(500))
        Turn(gun, y_axis, 0.0, math.rad(500))
        Turn(gun, z_axis, 0.0, math.rad(500))
        Turn(flare, x_axis, 0.0, math.rad(500))
        Turn(flare, y_axis, 0.0, math.rad(500))
        Turn(flare, z_axis, 0.0, math.rad(500))

        WaitForTurn(base, x_axis)
        WaitForTurn(base, y_axis)
        WaitForTurn(base, z_axis)
        WaitForTurn(pelvis, x_axis)
        WaitForTurn(pelvis, y_axis)
        WaitForTurn(pelvis, z_axis)
        WaitForTurn(lthigh, x_axis)
        WaitForTurn(lthigh, y_axis)
        WaitForTurn(lthigh, z_axis)
        WaitForTurn(lleg, x_axis)
        WaitForTurn(lleg, y_axis)
        WaitForTurn(lleg, z_axis)
        WaitForTurn(rthigh, x_axis)
        WaitForTurn(rthigh, y_axis)
        WaitForTurn(rthigh, z_axis)
        WaitForTurn(rleg, x_axis)
        WaitForTurn(rleg, y_axis)
        WaitForTurn(rleg, z_axis)
        WaitForTurn(torso, x_axis)
        WaitForTurn(torso, y_axis)
        WaitForTurn(torso, z_axis)
        WaitForTurn(chest, x_axis)
        WaitForTurn(chest, y_axis)
        WaitForTurn(chest, z_axis)
        WaitForTurn(head, x_axis)
        WaitForTurn(head, y_axis)
        WaitForTurn(head, z_axis)
        WaitForTurn(head2, x_axis)
        WaitForTurn(head2, y_axis)
        WaitForTurn(head2, z_axis)
        WaitForTurn(head3, x_axis)
        WaitForTurn(head3, y_axis)
        WaitForTurn(head3, z_axis)
        WaitForTurn(helmet, x_axis)
        WaitForTurn(helmet, y_axis)
        WaitForTurn(helmet, z_axis)
        WaitForTurn(shoulders, x_axis)
        WaitForTurn(shoulders, y_axis)
        WaitForTurn(shoulders, z_axis)
        WaitForTurn(luparm, x_axis)
        WaitForTurn(luparm, y_axis)
        WaitForTurn(luparm, z_axis)
        WaitForTurn(lloarm, x_axis)
        WaitForTurn(lloarm, y_axis)
        WaitForTurn(lloarm, z_axis)
        WaitForTurn(ruparm, x_axis)
        WaitForTurn(ruparm, y_axis)
        WaitForTurn(ruparm, z_axis)
        WaitForTurn(rloarm, x_axis)
        WaitForTurn(rloarm, y_axis)
        WaitForTurn(rloarm, z_axis)
        WaitForTurn(gun, x_axis)
        WaitForTurn(gun, y_axis)
        WaitForTurn(gun, z_axis)
        WaitForTurn(flare, x_axis)
        WaitForTurn(flare, y_axis)
        WaitForTurn(flare, z_axis)
    elseif USE_POSE == 3 then
        Turn(base, x_axis, 0.0, math.rad(500))
        Turn(base, y_axis, 0.0, math.rad(500))
        Turn(base, z_axis, 0.0, math.rad(500))
        Turn(pelvis, x_axis, 0.0, math.rad(500))
        Turn(pelvis, y_axis, 0.0, math.rad(500))
        Turn(pelvis, z_axis, 0.0, math.rad(500))
        Turn(lthigh, x_axis, 0.0, math.rad(500))
        Turn(lthigh, y_axis, -0.0, math.rad(500))
        Turn(lthigh, z_axis, 0.0872664600610733, math.rad(500))
        Turn(lleg, x_axis, 0.0, math.rad(500))
        Turn(lleg, y_axis, -0.0, math.rad(500))
        Turn(lleg, z_axis, -0.0872664600610733, math.rad(500))
        Turn(rthigh, x_axis, 0.0, math.rad(500))
        Turn(rthigh, y_axis, -0.0, math.rad(500))
        Turn(rthigh, z_axis, -0.0872664600610733, math.rad(500))
        Turn(rleg, x_axis, 0.0, math.rad(500))
        Turn(rleg, y_axis, -0.0, math.rad(500))
        Turn(rleg, z_axis, 0.0872664600610733, math.rad(500))
        Turn(torso, x_axis, 0.0, math.rad(500))
        Turn(torso, y_axis, 0.0, math.rad(500))
        Turn(torso, z_axis, 0.0, math.rad(500))
        Turn(chest, x_axis, 0.0, math.rad(500))
        Turn(chest, y_axis, 0.0, math.rad(500))
        Turn(chest, z_axis, 0.0, math.rad(500))
        Turn(head, x_axis, 0.0, math.rad(500))
        Turn(head, y_axis, 0.0, math.rad(500))
        Turn(head, z_axis, 0.0, math.rad(500))
        Turn(head2, x_axis, 0.0, math.rad(500))
        Turn(head2, y_axis, 0.0, math.rad(500))
        Turn(head2, z_axis, 0.0, math.rad(500))
        Turn(head3, x_axis, 0.0, math.rad(500))
        Turn(head3, y_axis, 0.0, math.rad(500))
        Turn(head3, z_axis, 0.0, math.rad(500))
        Turn(helmet, x_axis, 0.0, math.rad(500))
        Turn(helmet, y_axis, 0.0, math.rad(500))
        Turn(helmet, z_axis, 0.0, math.rad(500))
        Turn(shoulders, x_axis, 0.0, math.rad(500))
        Turn(shoulders, y_axis, 0.0, math.rad(500))
        Turn(shoulders, z_axis, 0.0, math.rad(500))
        Turn(luparm, x_axis, 0.08693605661392212, math.rad(500))
        Turn(luparm, y_axis, -0.007596195675432682, math.rad(500))
        Turn(luparm, z_axis, 0.08693605661392212, math.rad(500))
        Turn(lloarm, x_axis, -0.250448077917099, math.rad(500))
        Turn(lloarm, y_axis, -0.07392090559005737, math.rad(500))
        Turn(lloarm, z_axis, -0.2511582672595978, math.rad(500))
        Turn(ruparm, x_axis, -0.1745329052209854, math.rad(500))
        Turn(ruparm, y_axis, -1.2987216857496264e-09, math.rad(500))
        Turn(ruparm, z_axis, -0.0872664600610733, math.rad(500))
        Turn(rloarm, x_axis, -0.34906578063964844, math.rad(500))
        Turn(rloarm, y_axis, 7.594372335373439e-17, math.rad(500))
        Turn(rloarm, z_axis, -1.3390929151600446e-17, math.rad(500))
        Turn(gun, x_axis, 0.0, math.rad(500))
        Turn(gun, y_axis, 0.0, math.rad(500))
        Turn(gun, z_axis, 0.0, math.rad(500))
        Turn(flare, x_axis, 0.0, math.rad(500))
        Turn(flare, y_axis, 0.0, math.rad(500))
        Turn(flare, z_axis, 0.0, math.rad(500))

        WaitForTurn(base, x_axis)
        WaitForTurn(base, y_axis)
        WaitForTurn(base, z_axis)
        WaitForTurn(pelvis, x_axis)
        WaitForTurn(pelvis, y_axis)
        WaitForTurn(pelvis, z_axis)
        WaitForTurn(lthigh, x_axis)
        WaitForTurn(lthigh, y_axis)
        WaitForTurn(lthigh, z_axis)
        WaitForTurn(lleg, x_axis)
        WaitForTurn(lleg, y_axis)
        WaitForTurn(lleg, z_axis)
        WaitForTurn(rthigh, x_axis)
        WaitForTurn(rthigh, y_axis)
        WaitForTurn(rthigh, z_axis)
        WaitForTurn(rleg, x_axis)
        WaitForTurn(rleg, y_axis)
        WaitForTurn(rleg, z_axis)
        WaitForTurn(torso, x_axis)
        WaitForTurn(torso, y_axis)
        WaitForTurn(torso, z_axis)
        WaitForTurn(chest, x_axis)
        WaitForTurn(chest, y_axis)
        WaitForTurn(chest, z_axis)
        WaitForTurn(head, x_axis)
        WaitForTurn(head, y_axis)
        WaitForTurn(head, z_axis)
        WaitForTurn(head2, x_axis)
        WaitForTurn(head2, y_axis)
        WaitForTurn(head2, z_axis)
        WaitForTurn(head3, x_axis)
        WaitForTurn(head3, y_axis)
        WaitForTurn(head3, z_axis)
        WaitForTurn(helmet, x_axis)
        WaitForTurn(helmet, y_axis)
        WaitForTurn(helmet, z_axis)
        WaitForTurn(shoulders, x_axis)
        WaitForTurn(shoulders, y_axis)
        WaitForTurn(shoulders, z_axis)
        WaitForTurn(luparm, x_axis)
        WaitForTurn(luparm, y_axis)
        WaitForTurn(luparm, z_axis)
        WaitForTurn(lloarm, x_axis)
        WaitForTurn(lloarm, y_axis)
        WaitForTurn(lloarm, z_axis)
        WaitForTurn(ruparm, x_axis)
        WaitForTurn(ruparm, y_axis)
        WaitForTurn(ruparm, z_axis)
        WaitForTurn(rloarm, x_axis)
        WaitForTurn(rloarm, y_axis)
        WaitForTurn(rloarm, z_axis)
        WaitForTurn(gun, x_axis)
        WaitForTurn(gun, y_axis)
        WaitForTurn(gun, z_axis)
        WaitForTurn(flare, x_axis)
        WaitForTurn(flare, y_axis)
        WaitForTurn(flare, z_axis)
    end
end

function AIMING_STEADY_POSE(heading, pitch)
    if USE_POSE <= 0 or USE_POSE > N_POSES then
        USE_POSE = 1
    end

    Turn(base, x_axis, 0.0, math.rad(500))
    Turn(base, y_axis, heading, math.rad(300))
    Turn(base, z_axis, 0.0, math.rad(500))
    if USE_POSE == 1 then
        Turn(pelvis, x_axis, 0.0, math.rad(500))
        Turn(pelvis, y_axis, 0.9599310755729675, math.rad(500))
        Turn(pelvis, z_axis, 0.0, math.rad(500))
        Turn(lthigh, x_axis, 0.09342743456363678, math.rad(500))
        Turn(lthigh, y_axis, 0.02438112534582615, math.rad(500))
        Turn(lthigh, z_axis, 0.2672315239906311, math.rad(500))
        Turn(lleg, x_axis, 0.17608235776424408, math.rad(500))
        Turn(lleg, y_axis, 0.19980432093143463, math.rad(500))
        Turn(lleg, z_axis, 0.005151008255779743, math.rad(500))
        Turn(rthigh, x_axis, -0.1833024024963379, math.rad(500))
        Turn(rthigh, y_axis, -0.6190490126609802, math.rad(500))
        Turn(rthigh, z_axis, -0.10890621691942215, math.rad(500))
        Turn(rleg, x_axis, 0.2456892430782318, math.rad(500))
        Turn(rleg, y_axis, -0.010951470583677292, math.rad(500))
        Turn(rleg, z_axis, 0.0886426642537117, math.rad(500))
        Turn(torso, x_axis, 0.0, math.rad(500))
        Turn(torso, y_axis, 0.0, math.rad(500))
        Turn(torso, z_axis, 0.0, math.rad(500))
        Turn(chest, x_axis, 0.0, math.rad(200))
        Turn(chest, y_axis, 0.0, math.rad(200))
        Turn(chest, z_axis, 0.0, math.rad(200))
        Turn(head, x_axis, -pitch, math.rad(200))
        Turn(head, y_axis, -0.9599311351776123, math.rad(200))
        Turn(head, z_axis, 0.0, math.rad(200))
        Turn(head2, x_axis, 0.0, math.rad(200))
        Turn(head2, y_axis, 0.0, math.rad(200))
        Turn(head2, z_axis, 0.0, math.rad(200))
        Turn(head3, x_axis, 0.0, math.rad(200))
        Turn(head3, y_axis, 0.0, math.rad(200))
        Turn(head3, z_axis, 0.0, math.rad(200))
        Turn(helmet, x_axis, 0.0, math.rad(200))
        Turn(helmet, y_axis, 0.0, math.rad(200))
        Turn(helmet, z_axis, 0.0, math.rad(200))
        Turn(shoulders, x_axis, 0.0, math.rad(500))
        Turn(shoulders, y_axis, 0.0, math.rad(500))
        Turn(shoulders, z_axis, 0.0, math.rad(500))
        Turn(luparm, x_axis, 0.14902248978614807, math.rad(500))
        Turn(luparm, y_axis, -0.01601034216582775, math.rad(500))
        Turn(luparm, z_axis, 0.21366126835346222, math.rad(500))
        Turn(lloarm, x_axis, -0.05001141503453255, math.rad(500))
        Turn(lloarm, y_axis, -0.001787872053682804, math.rad(500))
        Turn(lloarm, z_axis, -0.071454718708992, math.rad(500))
        Turn(ruparm, x_axis, -1.570796251296997 - pitch, math.rad(500))
        Turn(ruparm, y_axis, -0.9599310755729675, math.rad(500))
        Turn(ruparm, z_axis, 0.0, math.rad(500))
        Turn(rloarm, x_axis, 0.0, math.rad(500))
        Turn(rloarm, y_axis, 0.0, math.rad(500))
        Turn(rloarm, z_axis, 0.0, math.rad(500))
        Turn(gun, x_axis, 0.0, math.rad(500))
        Turn(gun, y_axis, 0.0, math.rad(500))
        Turn(gun, z_axis, 0.0, math.rad(500))
        Turn(flare, x_axis, 0.0, math.rad(500))
        Turn(flare, y_axis, 0.0, math.rad(500))
        Turn(flare, z_axis, 0.0, math.rad(500))

        WaitForTurn(pelvis, x_axis)
        WaitForTurn(pelvis, y_axis)
        WaitForTurn(pelvis, z_axis)
        WaitForTurn(lthigh, x_axis)
        WaitForTurn(lthigh, y_axis)
        WaitForTurn(lthigh, z_axis)
        WaitForTurn(lleg, x_axis)
        WaitForTurn(lleg, y_axis)
        WaitForTurn(lleg, z_axis)
        WaitForTurn(rthigh, x_axis)
        WaitForTurn(rthigh, y_axis)
        WaitForTurn(rthigh, z_axis)
        WaitForTurn(rleg, x_axis)
        WaitForTurn(rleg, y_axis)
        WaitForTurn(rleg, z_axis)
        WaitForTurn(torso, x_axis)
        WaitForTurn(torso, y_axis)
        WaitForTurn(torso, z_axis)
        WaitForTurn(chest, x_axis)
        WaitForTurn(chest, y_axis)
        WaitForTurn(chest, z_axis)
        WaitForTurn(head, x_axis)
        WaitForTurn(head, y_axis)
        WaitForTurn(head, z_axis)
        WaitForTurn(head2, x_axis)
        WaitForTurn(head2, y_axis)
        WaitForTurn(head2, z_axis)
        WaitForTurn(head3, x_axis)
        WaitForTurn(head3, y_axis)
        WaitForTurn(head3, z_axis)
        WaitForTurn(helmet, x_axis)
        WaitForTurn(helmet, y_axis)
        WaitForTurn(helmet, z_axis)
        WaitForTurn(shoulders, x_axis)
        WaitForTurn(shoulders, y_axis)
        WaitForTurn(shoulders, z_axis)
        WaitForTurn(luparm, x_axis)
        WaitForTurn(luparm, y_axis)
        WaitForTurn(luparm, z_axis)
        WaitForTurn(lloarm, x_axis)
        WaitForTurn(lloarm, y_axis)
        WaitForTurn(lloarm, z_axis)
        WaitForTurn(ruparm, x_axis)
        WaitForTurn(ruparm, y_axis)
        WaitForTurn(ruparm, z_axis)
        WaitForTurn(rloarm, x_axis)
        WaitForTurn(rloarm, y_axis)
        WaitForTurn(rloarm, z_axis)
        WaitForTurn(gun, x_axis)
        WaitForTurn(gun, y_axis)
        WaitForTurn(gun, z_axis)
        WaitForTurn(flare, x_axis)
        WaitForTurn(flare, y_axis)
        WaitForTurn(flare, z_axis)
    elseif USE_POSE == 2 then
        Turn(pelvis, x_axis, 0.0, math.rad(500))
        Turn(pelvis, y_axis, 0.0, math.rad(500))
        Turn(pelvis, z_axis, 0.0, math.rad(500))
        Turn(lthigh, x_axis, 0.17388209700584412, math.rad(500))
        Turn(lthigh, y_axis, -0.015135012567043304, math.rad(500))
        Turn(lthigh, z_axis, 0.08594724535942078, math.rad(500))
        Turn(lleg, x_axis, 1.8913792754204906e-09, math.rad(500))
        Turn(lleg, y_axis, 0.1745329350233078, math.rad(500))
        Turn(lleg, z_axis, -0.08726644515991211, math.rad(500))
        Turn(rthigh, x_axis, -0.17388209700584412, math.rad(500))
        Turn(rthigh, y_axis, -0.015135012567043304, math.rad(500))
        Turn(rthigh, z_axis, -0.08594724535942078, math.rad(500))
        Turn(rleg, x_axis, 0.17518608272075653, math.rad(500))
        Turn(rleg, y_axis, -0.08593737334012985, math.rad(500))
        Turn(rleg, z_axis, 0.07207536697387695, math.rad(500))
        Turn(torso, x_axis, 0.1745329350233078, math.rad(500))
        Turn(torso, y_axis, -0.3490658700466156, math.rad(500))
        Turn(torso, z_axis, -1.1666245747221637e-08, math.rad(500))
        Turn(chest, x_axis, 0.0, math.rad(500))
        Turn(chest, y_axis, 0.0, math.rad(500))
        Turn(chest, z_axis, 0.0, math.rad(500))
        Turn(head, x_axis, -0.1745329201221466 - pitch, math.rad(500))
        Turn(head, y_axis, 0.3490658402442932, math.rad(500))
        Turn(head, z_axis, 0.0, math.rad(500))
        Turn(head2, x_axis, 0.0, math.rad(500))
        Turn(head2, y_axis, 0.0, math.rad(500))
        Turn(head2, z_axis, 0.0, math.rad(500))
        Turn(head3, x_axis, 0.0, math.rad(500))
        Turn(head3, y_axis, 0.0, math.rad(500))
        Turn(head3, z_axis, 0.0, math.rad(500))
        Turn(helmet, x_axis, 0.0, math.rad(500))
        Turn(helmet, y_axis, 0.0, math.rad(500))
        Turn(helmet, z_axis, 0.0, math.rad(500))
        Turn(shoulders, x_axis, -pitch, math.rad(500))
        Turn(shoulders, y_axis, 0.0, math.rad(500))
        Turn(shoulders, z_axis, 0.0, math.rad(500))
        Turn(luparm, x_axis, -1.489111304283142, math.rad(500))
        Turn(luparm, y_axis, -0.09086450189352036, math.rad(500))
        Turn(luparm, z_axis, -0.02149232290685177, math.rad(500))
        Turn(lloarm, x_axis, -0.4944100081920624, math.rad(500))
        Turn(lloarm, y_axis, -0.17558553814888, math.rad(500))
        Turn(lloarm, z_axis, 0.07603959739208221, math.rad(500))
        Turn(ruparm, x_axis, -1.5707963705062866, math.rad(500))
        Turn(ruparm, y_axis, 0.4837486445903778, math.rad(500))
        Turn(ruparm, z_axis, 0.18743400275707245, math.rad(500))
        Turn(rloarm, x_axis, -0.14357593655586243, math.rad(500))
        Turn(rloarm, y_axis, 0.2633579671382904, math.rad(500))
        Turn(rloarm, z_axis, -0.0003270621527917683, math.rad(500))
        Turn(gun, x_axis, 0.001716173836030066, math.rad(500))
        Turn(gun, y_axis, 0.022789273411035538, math.rad(500))
        Turn(gun, z_axis, -0.29740992188453674, math.rad(500))
        Turn(flare, x_axis, 0.0, math.rad(500))
        Turn(flare, y_axis, 0.0, math.rad(500))
        Turn(flare, z_axis, 0.0, math.rad(500))

        WaitForTurn(pelvis, x_axis)
        WaitForTurn(pelvis, y_axis)
        WaitForTurn(pelvis, z_axis)
        WaitForTurn(lthigh, x_axis)
        WaitForTurn(lthigh, y_axis)
        WaitForTurn(lthigh, z_axis)
        WaitForTurn(lleg, x_axis)
        WaitForTurn(lleg, y_axis)
        WaitForTurn(lleg, z_axis)
        WaitForTurn(rthigh, x_axis)
        WaitForTurn(rthigh, y_axis)
        WaitForTurn(rthigh, z_axis)
        WaitForTurn(rleg, x_axis)
        WaitForTurn(rleg, y_axis)
        WaitForTurn(rleg, z_axis)
        WaitForTurn(torso, x_axis)
        WaitForTurn(torso, y_axis)
        WaitForTurn(torso, z_axis)
        WaitForTurn(chest, x_axis)
        WaitForTurn(chest, y_axis)
        WaitForTurn(chest, z_axis)
        WaitForTurn(head, x_axis)
        WaitForTurn(head, y_axis)
        WaitForTurn(head, z_axis)
        WaitForTurn(head2, x_axis)
        WaitForTurn(head2, y_axis)
        WaitForTurn(head2, z_axis)
        WaitForTurn(head3, x_axis)
        WaitForTurn(head3, y_axis)
        WaitForTurn(head3, z_axis)
        WaitForTurn(helmet, x_axis)
        WaitForTurn(helmet, y_axis)
        WaitForTurn(helmet, z_axis)
        WaitForTurn(shoulders, x_axis)
        WaitForTurn(shoulders, y_axis)
        WaitForTurn(shoulders, z_axis)
        WaitForTurn(luparm, x_axis)
        WaitForTurn(luparm, y_axis)
        WaitForTurn(luparm, z_axis)
        WaitForTurn(lloarm, x_axis)
        WaitForTurn(lloarm, y_axis)
        WaitForTurn(lloarm, z_axis)
        WaitForTurn(ruparm, x_axis)
        WaitForTurn(ruparm, y_axis)
        WaitForTurn(ruparm, z_axis)
        WaitForTurn(rloarm, x_axis)
        WaitForTurn(rloarm, y_axis)
        WaitForTurn(rloarm, z_axis)
        WaitForTurn(gun, x_axis)
        WaitForTurn(gun, y_axis)
        WaitForTurn(gun, z_axis)
        WaitForTurn(flare, x_axis)
        WaitForTurn(flare, y_axis)
        WaitForTurn(flare, z_axis)
    elseif USE_POSE == 3 then
        Turn(pelvis, x_axis, 0.0, math.rad(500))
        Turn(pelvis, y_axis, 0.0, math.rad(500))
        Turn(pelvis, z_axis, 0.0, math.rad(500))
        Turn(lthigh, x_axis, -0.08693605661392212, math.rad(500))
        Turn(lthigh, y_axis, 0.007596195675432682, math.rad(500))
        Turn(lthigh, z_axis, 0.08693605661392212, math.rad(500))
        Turn(lleg, x_axis, 0.0872664600610733, math.rad(500))
        Turn(lleg, y_axis, 4.656612873077393e-10, math.rad(500))
        Turn(lleg, z_axis, -0.0872664675116539, math.rad(500))
        Turn(rthigh, x_axis, -0.08693605661392212, math.rad(500))
        Turn(rthigh, y_axis, -0.007596195675432682, math.rad(500))
        Turn(rthigh, z_axis, -0.08693605661392212, math.rad(500))
        Turn(rleg, x_axis, 0.0872664600610733, math.rad(500))
        Turn(rleg, y_axis, -4.656612873077393e-10, math.rad(500))
        Turn(rleg, z_axis, 0.0872664675116539, math.rad(500))
        Turn(torso, x_axis, 0.0, math.rad(500))
        Turn(torso, y_axis, 0.0, math.rad(500))
        Turn(torso, z_axis, 0.0, math.rad(500))
        Turn(chest, x_axis, 0.0, math.rad(500))
        Turn(chest, y_axis, 0.0, math.rad(500))
        Turn(chest, z_axis, 0.0, math.rad(500))
        Turn(head, x_axis, -pitch, math.rad(500))
        Turn(head, y_axis, 0.0, math.rad(500))
        Turn(head, z_axis, 0.0, math.rad(500))
        Turn(head2, x_axis, 0.0, math.rad(500))
        Turn(head2, y_axis, 0.0, math.rad(500))
        Turn(head2, z_axis, 0.0, math.rad(500))
        Turn(head3, x_axis, 0.0, math.rad(500))
        Turn(head3, y_axis, 0.0, math.rad(500))
        Turn(head3, z_axis, 0.0, math.rad(500))
        Turn(helmet, x_axis, 0.0, math.rad(500))
        Turn(helmet, y_axis, 0.0, math.rad(500))
        Turn(helmet, z_axis, 0.0, math.rad(500))
        Turn(shoulders, x_axis, 0.0, math.rad(500))
        Turn(shoulders, y_axis, 0.0, math.rad(500))
        Turn(shoulders, z_axis, 0.0, math.rad(500))
        Turn(luparm, x_axis, 0.2557127773761749, math.rad(500))
        Turn(luparm, y_axis, -0.052391424775123596, math.rad(500))
        Turn(luparm, z_axis, 0.2563941478729248, math.rad(500))
        Turn(lloarm, x_axis, -0.7662831544876099, math.rad(500))
        Turn(lloarm, y_axis, 0.038431789726018906, math.rad(500))
        Turn(lloarm, z_axis, -0.07995955646038055, math.rad(500))
        Turn(ruparm, x_axis, -0.4363322854042053, math.rad(500))
        Turn(ruparm, y_axis, -1.2510454894254508e-09, math.rad(500))
        Turn(ruparm, z_axis, 3.352166877412799e-10, math.rad(500))
        Turn(rloarm, x_axis, -1.134463906288147 - pitch, math.rad(500))
        Turn(rloarm, y_axis, 0.0, math.rad(500))
        Turn(rloarm, z_axis, 0.0, math.rad(500))
        Turn(gun, x_axis, 0.0, math.rad(500))
        Turn(gun, y_axis, 0.0, math.rad(500))
        Turn(gun, z_axis, 0.0, math.rad(500))
        Turn(flare, x_axis, 0.0, math.rad(500))
        Turn(flare, y_axis, 0.0, math.rad(500))
        Turn(flare, z_axis, 0.0, math.rad(500))

        WaitForTurn(pelvis, x_axis)
        WaitForTurn(pelvis, y_axis)
        WaitForTurn(pelvis, z_axis)
        WaitForTurn(lthigh, x_axis)
        WaitForTurn(lthigh, y_axis)
        WaitForTurn(lthigh, z_axis)
        WaitForTurn(lleg, x_axis)
        WaitForTurn(lleg, y_axis)
        WaitForTurn(lleg, z_axis)
        WaitForTurn(rthigh, x_axis)
        WaitForTurn(rthigh, y_axis)
        WaitForTurn(rthigh, z_axis)
        WaitForTurn(rleg, x_axis)
        WaitForTurn(rleg, y_axis)
        WaitForTurn(rleg, z_axis)
        WaitForTurn(torso, x_axis)
        WaitForTurn(torso, y_axis)
        WaitForTurn(torso, z_axis)
        WaitForTurn(chest, x_axis)
        WaitForTurn(chest, y_axis)
        WaitForTurn(chest, z_axis)
        WaitForTurn(head, x_axis)
        WaitForTurn(head, y_axis)
        WaitForTurn(head, z_axis)
        WaitForTurn(head2, x_axis)
        WaitForTurn(head2, y_axis)
        WaitForTurn(head2, z_axis)
        WaitForTurn(head3, x_axis)
        WaitForTurn(head3, y_axis)
        WaitForTurn(head3, z_axis)
        WaitForTurn(helmet, x_axis)
        WaitForTurn(helmet, y_axis)
        WaitForTurn(helmet, z_axis)
        WaitForTurn(shoulders, x_axis)
        WaitForTurn(shoulders, y_axis)
        WaitForTurn(shoulders, z_axis)
        WaitForTurn(luparm, x_axis)
        WaitForTurn(luparm, y_axis)
        WaitForTurn(luparm, z_axis)
        WaitForTurn(lloarm, x_axis)
        WaitForTurn(lloarm, y_axis)
        WaitForTurn(lloarm, z_axis)
        WaitForTurn(ruparm, x_axis)
        WaitForTurn(ruparm, y_axis)
        WaitForTurn(ruparm, z_axis)
        WaitForTurn(rloarm, x_axis)
        WaitForTurn(rloarm, y_axis)
        WaitForTurn(rloarm, z_axis)
        WaitForTurn(gun, x_axis)
        WaitForTurn(gun, y_axis)
        WaitForTurn(gun, z_axis)
        WaitForTurn(flare, x_axis)
        WaitForTurn(flare, y_axis)
        WaitForTurn(flare, z_axis)
    end
    WaitForTurn(base, x_axis)
    WaitForTurn(base, y_axis)
    WaitForTurn(base, z_axis)

    return true
end

function AIMING_RUN_POSE(heading, pitch)
    if heading < -1.570796251296997 or heading > -1.570796251296997 then
        -- We can not aim the target
        return false
    end

    Turn(torso, x_axis, 0.0, math.rad(500))
    Turn(torso, y_axis, heading / 2, math.rad(500))
    Turn(torso, z_axis, 0.0, math.rad(500))
    Turn(chest, x_axis, 0.0, math.rad(200))
    Turn(chest, y_axis, 0.0, math.rad(200))
    Turn(chest, z_axis, 0.0, math.rad(200))
    Turn(head, x_axis, 3.536157677785923e-08, math.rad(200))
    Turn(head, y_axis, -0.9599311351776123 - pitch, math.rad(200))
    Turn(head, z_axis, -4.067665670959286e-08, math.rad(200))
    Turn(head2, x_axis, 0.0, math.rad(200))
    Turn(head2, y_axis, 0.0, math.rad(200))
    Turn(head2, z_axis, 0.0, math.rad(200))
    Turn(head3, x_axis, 0.0, math.rad(200))
    Turn(head3, y_axis, 0.0, math.rad(200))
    Turn(head3, z_axis, 0.0, math.rad(200))
    Turn(helmet, x_axis, 0.0, math.rad(200))
    Turn(helmet, y_axis, 0.0, math.rad(200))
    Turn(helmet, z_axis, 0.0, math.rad(200))
    Turn(shoulders, x_axis, 0.0, math.rad(500))
    Turn(shoulders, y_axis, 0.0, math.rad(500))
    Turn(shoulders, z_axis, 0.0, math.rad(500))
    Turn(luparm, x_axis, 0.14902248978614807, math.rad(500))
    Turn(luparm, y_axis, -0.01601034216582775, math.rad(500))
    Turn(luparm, z_axis, 0.21366126835346222, math.rad(500))
    Turn(lloarm, x_axis, -0.05001141503453255, math.rad(500))
    Turn(lloarm, y_axis, -0.001787872053682804, math.rad(500))
    Turn(lloarm, z_axis, -0.071454718708992, math.rad(500))
    Turn(ruparm, x_axis, -1.570796251296997 - pitch, math.rad(500))
    Turn(ruparm, y_axis, heading / 2, math.rad(500))
    Turn(ruparm, z_axis, 0.0, math.rad(500))
    Turn(rloarm, x_axis, 0.0, math.rad(500))
    Turn(rloarm, y_axis, 0.0, math.rad(500))
    Turn(rloarm, z_axis, 0.0, math.rad(500))
    Turn(gun, x_axis, 0.0, math.rad(500))
    Turn(gun, y_axis, 0.0, math.rad(500))
    Turn(gun, z_axis, 0.0, math.rad(500))
    Turn(flare, x_axis, 0.0, math.rad(500))
    Turn(flare, y_axis, 0.0, math.rad(500))
    Turn(flare, z_axis, 0.0, math.rad(500))

    WaitForTurn(torso, x_axis)
    WaitForTurn(torso, y_axis)
    WaitForTurn(torso, z_axis)
    WaitForTurn(chest, x_axis)
    WaitForTurn(chest, y_axis)
    WaitForTurn(chest, z_axis)
    WaitForTurn(head, x_axis)
    WaitForTurn(head, y_axis)
    WaitForTurn(head, z_axis)
    WaitForTurn(head2, x_axis)
    WaitForTurn(head2, y_axis)
    WaitForTurn(head2, z_axis)
    WaitForTurn(head3, x_axis)
    WaitForTurn(head3, y_axis)
    WaitForTurn(head3, z_axis)
    WaitForTurn(helmet, x_axis)
    WaitForTurn(helmet, y_axis)
    WaitForTurn(helmet, z_axis)
    WaitForTurn(shoulders, x_axis)
    WaitForTurn(shoulders, y_axis)
    WaitForTurn(shoulders, z_axis)
    WaitForTurn(luparm, x_axis)
    WaitForTurn(luparm, y_axis)
    WaitForTurn(luparm, z_axis)
    WaitForTurn(lloarm, x_axis)
    WaitForTurn(lloarm, y_axis)
    WaitForTurn(lloarm, z_axis)
    WaitForTurn(ruparm, x_axis)
    WaitForTurn(ruparm, y_axis)
    WaitForTurn(ruparm, z_axis)
    WaitForTurn(rloarm, x_axis)
    WaitForTurn(rloarm, y_axis)
    WaitForTurn(rloarm, z_axis)
    WaitForTurn(gun, x_axis)
    WaitForTurn(gun, y_axis)
    WaitForTurn(gun, z_axis)
    WaitForTurn(flare, x_axis)
    WaitForTurn(flare, y_axis)
    WaitForTurn(flare, z_axis)

    return true
end
